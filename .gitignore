# Node.js
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.npm/
.yarn/
/npm-cache/

# Electron-specific
dist/             # Output of electron-builder
out/              # Another common output folder for Electron builds
release/
build/            # If you use a 'build' folder for temporary build assets (not for source code)

# OS-specific
.DS_Store         # macOS
Thumbs.db         # Windows
.vscode/          # VS Code editor settings (optional, but good to ignore)
*.env             # Environment variables (usually contain secrets)